function isPunctual(currentTime) {
  // Add your code here to check if the current time meets the required standard
  // For this example, let's assume the requirement is that the current time should be less than 15 minutes past midnight
  return currentTime < 90;
}
