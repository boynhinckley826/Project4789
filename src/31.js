// import { express } from 'express';
// const app = express();

// // API route to check the current time and status
// app.get('/status', (req, res) => {
//   let now = new Date();
//   let hours = String(now.getHours()).padStart(2, '0');
//   let minutes = String(now.getMinutes()).padStart(2, '0');

//   res.send(`The current time is ${hours}:${minutes} and the status is 'ready'`);
// });

// // API route to check the current date
// app.get('/date', (req, res) => {
//   let currentDate = new Date();
//   res.send(`The current date is ${currentDate.toISOString().split('T')[0]}`);
// });
